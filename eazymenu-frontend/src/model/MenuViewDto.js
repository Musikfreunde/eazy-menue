export default class MenuViewDto {
  constructor (id, date, code, appetizer, mainDish, dessert, categories) {
    this.id = id
    this.date = date
    this.code = code
    this.appetizer = appetizer
    this.mainDish = mainDish
    this.dessert = dessert
    this.categories = categories
  }
}
